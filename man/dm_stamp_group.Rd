% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population-filter-block.R
\name{dm_stamp_group}
\alias{dm_stamp_group}
\title{Stamp the board's group column}
\usage{
dm_stamp_group(data, col, as = "Group", groups = NULL)
}
\arguments{
\item{data}{A \link[dm:dm]{dm} or a data frame.}

\item{col}{Column to copy. A column no table carries is a no-op, so a board
whose study lacks it still renders.}

\item{as}{Name of the copy.}

\item{groups}{The group definition for \code{col}, as the population filter's
\code{groups} state holds it: \verb{list(show = <levels with a column of their own>, pools = list(list(name = , members = ), ...))}. \code{NULL} copies \code{col}
as it is. See \code{\link[=stamp_group]{stamp_group()}}.}
}
\value{
\code{data}, with the copy added.
}
\description{
Copies \code{col} into a column named \code{as} on the parent table of \code{data}, so that
every exhibit downstream can bind one name (\code{Group}) instead of the name of
whichever column the board is currently split by.
}
\details{
Only the parent table is stamped. Event tables reach the same value through
the join a flatten performs anyway, and stamping them too would give
\code{dm_flatten_to_tbl()} the same column twice: it renames the pair to
\code{Group.adsl} / \code{Group.ae} and every chart binding \code{Group} loses its column.
}
