% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population-filter-block.R
\name{new_population_filter_block}
\alias{new_population_filter_block}
\title{Population filter block}
\usage{
new_population_filter_block(featured = character(), pinned = NULL, ...)
}
\arguments{
\item{featured}{Columns worth showing up front, e.g.
\code{c("TRT", "SEX", "RACE", "AETOXGR")}.}

\item{pinned}{The column the board is split by at start, one of \code{featured}.}

\item{...}{Forwarded to \code{\link[blockr.dm:new_crossfilter_block]{blockr.dm::new_crossfilter_block()}}. Pass
\code{stamp_as} here to write the pick to a column other than \code{Group}, or
\code{stamp_as = NULL} for no stamp at all, which leaves the plain
crossfilter under this block's name.}
}
\value{
A crossfilter block carrying a \code{population_filter_block} subclass.
}
\description{
The crossfilter block with the two conventions a clinical board runs on: the
pinned card holds the study's stratifier, and whatever it holds is copied
into a \code{Group} column that every chart, table and model on the board binds
by name. Switching the pick in the card header re-points the whole board.
}
\details{
\code{featured} is the vocabulary the block offers up front: the columns that
appear as one-click chips, and, for those on the subject table, the choices
in the pinned card's picker. Everything else stays reachable through the
gear.
}
