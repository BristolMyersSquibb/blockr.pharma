% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population-filter-block.R
\name{stamp_group}
\alias{stamp_group}
\title{Build the board's group column from a group definition}
\usage{
stamp_group(x, col, groups = NULL)
}
\arguments{
\item{x}{The column the board is split by.}

\item{col}{Its name, kept as the \code{blockr_source} attribute.}

\item{groups}{\code{list(show = , pools = list(list(name = , members = ), ...))}
or \code{NULL}.}
}
\value{
The group column: character, carrying \code{blockr_source} and, for a
definition that changes anything, \code{blockr_groups} =
\verb{list(groups = <name -> raw members, in column order>, overlap = )}.
}
\description{
The population filter lets a reader pool, drop, rename and reorder the
levels of the column the board is split by. This turns that definition into
the \code{Group} column. Every subject keeps one row whatever the definition, so
nothing that counts across groups (a Total column, the crossfilter) can
count a subject twice.
}
\details{
\itemize{
\item No definition, or one that shows every level in level order with no
pools: a plain copy of \code{x}, as before groups existed.
\item A partition (no level in two groups): the group name, \code{NA} for a level in
no group.
\item Overlapping groups (a level in two, e.g. both doses pooled and the high
dose again on its own): the raw level. The groups travel as the
\code{blockr_groups} attribute and the consumers that split by \code{Group} expand
the rows themselves: composer tables through \code{\link[=group_by_args]{group_by_args()}}, charts
through \code{blockr.viz::expand_groups()}.
}
}
