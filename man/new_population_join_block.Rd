% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population-join-block.R
\name{new_population_join_block}
\alias{new_population_join_block}
\title{Population join block}
\usage{
new_population_join_block(id = "USUBJID", ...)
}
\arguments{
\item{id}{Subject identifier present in both inputs. Default \code{"USUBJID"}.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\description{
Two inputs: \code{data}, the events, and \code{population}, the subject-level table
they came from. Appends the subjects the events never mention, so a
downstream chart or table can divide by the population rather than by
whoever happens to appear in the event rows. See \code{\link[=join_population]{join_population()}} for the
exact rule.
}
\section{Where it goes}{
\strong{Last in the branch, immediately before the consumer.} Everything that
filters events -- the flag filter, value filters, a term cut -- belongs
upstream of it. A filter placed \emph{after} it would delete the appended rows
and put the denominator quietly back to subjects-with-an-event, which is
the bug this block exists to fix. That single link is the whole contract.
}

\section{What to do downstream}{
\itemize{
\item a chart: \code{func = "pct_distinct"} with \code{na_group = "drop"}, so the
appended rows draw no bar and still count
\item a summary table: derive the denominator by taking one row per subject
(the frame is unique on \code{id} once deduplicated, and every subject carries
the population's columns)
}

Both give the same N, which is the point of defining it once per branch.
}

\examples{
if (interactive()) {
  library(blockr.core)
  serve(
    new_population_join_block(),
    data = list(data = my_adae, population = my_adsl)
  )
}

}
