% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population-join-block.R
\name{join_population}
\alias{join_population}
\title{Restore the subjects an event table never mentions}
\usage{
join_population(events, population, id = "USUBJID")
}
\arguments{
\item{events}{Event-level data frame, already filtered.}

\item{population}{Subject-level data frame -- one row per subject, e.g.
ADSL restricted to a population flag.}

\item{id}{Subject identifier, present in both. Default \code{"USUBJID"}.}
}
\value{
\code{events} with the absent subjects appended.
}
\description{
Appends one row per subject that is present in \code{population} and absent from
\code{events}, carrying the population's columns and \code{NA} for everything
event-level. The result is the events \strong{plus their population}: counting
distinct subjects in it gives the denominator a rate needs, while the events
themselves are untouched.
}
\details{
Population columns the events already carry are left alone, so nothing is
renamed and no \code{.x} / \code{.y} suffixes appear. Columns the events lack are
joined on, which is how the appended rows get a treatment arm to be grouped
by. That asymmetry is deliberate: the events are the subject here, the
population only supplies what they are missing.
}
\examples{
ev  <- data.frame(USUBJID = c("S1", "S1", "S2"),
                  AEDECOD = c("Diarrhoea", "Diarrhoea", "Nausea"))
pop <- data.frame(USUBJID = c("S1", "S2", "S3"), TRT01A = "A")
join_population(ev, pop)

}
