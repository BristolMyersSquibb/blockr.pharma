% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pp-subject.R
\name{pp_pick_subject}
\alias{pp_pick_subject}
\title{Scope a dm to one subject, remembering the cohort}
\usage{
pp_pick_subject(data, subject)
}
\arguments{
\item{data}{A keyed dm holding the cohort (raw table names).}

\item{subject}{A single USUBJID.}
}
\value{
The subject-scoped dm, cohort attached as \code{attr(, "pp_cohort")}.
}
\description{
The patient profile block's output expression: filters every table to
\code{subject} (so a downstream block can never show the cohort while the
charts show one patient) while stashing the UNFILTERED input as the
\code{pp_cohort} attribute on the result. That attribute is what lets a
report or deck built over this block show the whole cohort -- the
block's true view -- even though its downstream data contract is the
picked patient: \code{\link[=pp_patient_exhibit]{pp_patient_exhibit()}} reads the attribute back when no
explicit subject is asked for. Ordinary dm operations downstream drop
the attribute, which is fine -- only the block's own result variable is
ever read for exhibits.
}
