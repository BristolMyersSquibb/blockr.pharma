% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population-filter-block.R
\name{add_total_group_denominator}
\alias{add_total_group_denominator}
\title{Add explicit group-pool rows to a composer denominator}
\usage{
add_total_group_denominator(
  denominator,
  col = "Group",
  total = "All Patients"
)
}
\arguments{
\item{denominator}{A data frame, typically from \code{composer::make_denom()}.}

\item{col}{Grouping column to overwrite in the duplicated rows.}

\item{total}{Total level name. \code{NULL} returns \code{denominator} unchanged.}
}
\value{
\code{denominator}, plus duplicated rows with \code{col} set to the synthetic
group names.
}
\description{
\code{composer::by(pools = )} can create a total column from raw rows for a
table-level denominator, but a denominator passed directly to a block is
validated as-is. This helper makes synthetic group levels explicit there.
}
