% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pp-exhibit.R
\name{pp_patient_exhibit}
\alias{pp_patient_exhibit}
\title{The Patient Profile, Rebuilt as Static Exhibits}
\usage{
pp_patient_exhibit(
  data,
  selected = NULL,
  viz_settings = list(),
  timeline_mode = c("rday", "date"),
  show_prestudy = FALSE,
  smooth = c("auto", "off"),
  subject = NULL,
  roles = NULL,
  scale_map = NULL,
  title = NULL,
  max_subjects = getOption("blockr.pharma.exhibit_max_subjects", 25L)
)
}
\arguments{
\item{data}{A \code{dm} of ADaM tables (the patient profile block's result):
one subject, or a cohort.}

\item{selected}{Character vector of viz ids to render, in order. \code{NULL}
falls back to the block's own default selection.}

\item{viz_settings}{Named list of per-viz settings, keyed by viz id --
the block's \code{viz_settings} state.}

\item{timeline_mode}{\code{"rday"} (relative day) or \code{"date"}; falls back to
\code{"date"} per patient when that subject has no timeline reference.}

\item{show_prestudy}{Show the full pre-treatment history instead of
clipping to the screening window.}

\item{smooth}{Findings line smoothing, \code{"auto"} or \code{"off"} (static lines
are straight segments either way; kept for state parity).}

\item{subject}{USUBJID to scope a cohort to one patient. \code{NULL} renders
the whole cohort.}

\item{roles}{Declared study roles (the \code{"study_roles"} board option
value), or \code{NULL} for the package conventions.}

\item{scale_map}{A blockr.theme board scale map for the severity /
indication colors, or \code{NULL} for the built-in constants.}

\item{title}{Optional exhibit title (used by the slide methods).}

\item{max_subjects}{Cap on how many patients a cohort renders
(default \code{getOption("blockr.pharma.exhibit_max_subjects", 25L)}): a
deck over an unfiltered 300-patient study is a mistake, not a review.}
}
\value{
An object of class \code{c("pp_exhibit", "blockr_exhibit")} whose
\code{patients} field holds, per rendered subject, the ggplots and their
labels -- or \code{NULL} when nothing can be rendered.
}
\description{
Re-derives the patient profile's selected visualizations server-side as
ggplots -- the printed form of the block, built from the same state the
block serializes (\code{selected}, \code{viz_settings}, \code{timeline_mode}, ...). This
is what the block's download buttons write and what a blockr.outline deck
places on slides: the export pipeline never captures the live echarts
canvases, it re-renders from state, exactly as the chart block's
\code{\link[blockr.viz:static_chart]{blockr.viz::static_chart()}} path does.
}
\details{
Handed a single-subject dm (the block's result once a patient is
picked), it renders that patient. Handed a cohort, it renders EVERY
subject -- one group of visuals per patient, so a deck over a
drilled-down cohort becomes a per-patient review -- up to
\code{max_subjects}, and says on the console what it dropped past the cap.

Visuals without a static twin (currently the NPI-X radar and the cycle
band) are skipped with a message, as is any visual whose data
requirements the dm does not meet.
}
