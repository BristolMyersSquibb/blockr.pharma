% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study-roles-option.R
\name{new_study_roles_option}
\alias{new_study_roles_option}
\title{Study roles: a board option}
\usage{
new_study_roles_option(
  arm = "",
  severity = "",
  timeline = "",
  category = "Study",
  ...
)
}
\arguments{
\item{arm}{ADSL arm column; \code{""} = undeclared (convention: \code{ACTARM}).}

\item{severity}{ADAE severity column; \code{""} = undeclared (convention:
detect \code{AETOXGR}, then \code{AESEV}).}

\item{timeline}{ADSL column anchoring relative-day mode; \code{""} =
undeclared (convention: \code{TRTSDT}).}

\item{category}{Settings sidebar category}

\item{...}{Forwarded to \code{\link[blockr.core:new_board_options]{blockr.core::new_board_option()}}}
}
\value{
A \code{board_option} with id \code{"study_roles"}.
}
\description{
Declares, per study board, which columns carry the profile's clinical
roles. The value serializes with the board, is editable in the settings
sidebar (category "Study"), and is deliberately not block state: a
constructor formal would round-trip through \code{state} and become reachable
by the AI assistant, and these are study facts, not analysis choices.
Board options are structurally invisible to the assistant.
}
\details{
Every field is optional. An empty field means the package convention; a
declared column that the data does not carry is a named error at
resolution, never a fallback -- a study that declared and got it wrong
must stop, not draw something plausible.
}
